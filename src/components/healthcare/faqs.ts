/**
 * Healthcare FAQs.
 *
 * TODO: write these. This file previously held a verbatim copy of the
 * customer-loyalty FAQs (CSAT, NPS, retention cadence) — wrong subject for this
 * page. Nothing rendered them, so the mistake was invisible, but the healthcare
 * route was emitting that FAQPage schema to search engines.
 *
 * Leaving it empty means the page renders no FAQ section and emits no FAQPage
 * markup, which is correct until real healthcare questions are supplied.
 */

export const faqs: { q: string; a: string }[] = [];
