# Base URL of the Track Opinion backend API.
# Server-side only — deliberately NOT prefixed with NEXT_PUBLIC_ so the
# upstream host is never inlined into the client bundle.
API_BASE_URL=https://trackapi.opinionest.com

# Google Search Console verification token.
# Leave unset locally; the <meta> tag is omitted when this is empty.
GOOGLE_SITE_VERIFICATION=
